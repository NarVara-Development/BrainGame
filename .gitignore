# ============================================================
# Dependencies
# ============================================================
node_modules/

# ============================================================
# Expo / build output
# ============================================================
.expo/
dist/
dist-check/
web-build/
expo-env.d.ts
*.tsbuildinfo

# Native projects (regenerated by `expo prebuild` / EAS — Continuous Native Generation)
android/
ios/

# ============================================================
# Environment variables & SECRETS (jangan pernah di-push)
# ============================================================
# boleh commit .env.example (tanpa nilai asli), tapi tidak .env asli
.env
.env.*
!.env.example

# Firebase / Google services (berisi API key & config)
google-services.json
GoogleService-Info.plist

# Play Store / signing credentials
play-service-account.json
*.keystore
*.jks
*.p8
*.p12
*.mobileprovision

# ============================================================
# Build artifacts (besar, hasil build — tidak perlu di-git)
# ============================================================
*.aab
*.apk
*.ipa
*.aar

# ============================================================
# Logs
# ============================================================
*.log
npm-debug.*
yarn-debug.*
yarn-error.*

# ============================================================
# OS / Editor junk
# ============================================================
.DS_Store
Thumbs.db
*.orig.*
.idea/
*.iml
.vscode/
